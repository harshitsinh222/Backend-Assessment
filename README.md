### Usage

```
1. In the terminal run "npm install"
2. Run the npm start
3. For Step 1, Go to http://localhost:3000/api/ping
4. For Step 2, Go to  http://localhost:3000/api/posts/history to get a result and can also pass parameters.
5. I have used cache in the postman.
```
